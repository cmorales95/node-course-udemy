const name = 'Deadpool';
const realName = 'Wade Winston';

const template = `${name} ${realName}`;

console.log(template);

const html = `
<h1>Hola</h1>
<p>Mundo</p>;
`

console.log(html);